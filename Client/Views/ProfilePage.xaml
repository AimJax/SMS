<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SocialMediaSimulator.Client.Views.ProfilePage"
             Title="Profile"
             BackgroundColor="#1A1A24">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="LogoutButton" Text="Logout" Clicked="OnLogoutClicked"/>
    </ContentPage.ToolbarItems>

    <ScrollView BackgroundColor="#1A1A24">
        <VerticalStackLayout Spacing="15" Padding="15">
            <!-- Profile Header -->
            <Frame Padding="20" BackgroundColor="#242430" CornerRadius="12">
                <VerticalStackLayout HorizontalOptions="Center">
                    <Frame WidthRequest="100" HeightRequest="100" CornerRadius="50" Padding="0" BackgroundColor="#333344">
                        <Label Text="{Binding Account.Profile.DisplayName[0]}" FontSize="42" FontAttributes="Bold" 
                               TextColor="#F5A623" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Frame>
                    <Label Text="{Binding Account.Profile.DisplayName}" FontSize="22" FontAttributes="Bold" 
                           TextColor="#F5F5F7" HorizontalOptions="Center" Margin="0,10,0,0"/>
                    <Label Text="{Binding Account.Username, StringFormat='@{0}'}" FontSize="14" TextColor="#9E9EA8"
                           HorizontalOptions="Center"/>
                    <Label Text="{Binding Account.Profile.Bio}" FontSize="14" TextColor="#EDEDED"
                           HorizontalOptions="Center" HorizontalTextAlignment="Center"
                           Margin="0,5,0,0" MaxLines="3"/>

                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="30" Margin="0,15,0,0">
                        <VerticalStackLayout HorizontalOptions="Center">
                            <Label Text="{Binding Account.Profile.FollowerCount}" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#F5F5F7"/>
                            <Label Text="Followers" FontSize="12" TextColor="#606070" HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout HorizontalOptions="Center">
                            <Label Text="{Binding Account.Profile.FollowingCount}" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" TextColor="#F5F5F7"/>
                            <Label Text="Following" FontSize="12" TextColor="#606070" HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </HorizontalStackLayout>

                    <Button Text="{Binding IsFollowing, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Following|Follow'}"
                            Command="{Binding FollowCommand}"
                            IsVisible="{Binding IsOwnProfile, Converter={StaticResource InverseBoolConverter}}"
                            Margin="0,15,0,0"
                            WidthRequest="120"
                            BackgroundColor="#333344"
                            TextColor="#F5A623"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Posts Section -->
            <Label Text="Posts" FontSize="18" FontAttributes="Bold" TextColor="#F5F5F7"/>
            <ScrollView HeightRequest="300" BackgroundColor="#1A1A24">
                <VerticalStackLayout x:Name="PostsLayout"/>
            </ScrollView>

            <ActivityIndicator x:Name="LoadingIndicator" IsRunning="False" HorizontalOptions="Center" Color="#F5A623"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
