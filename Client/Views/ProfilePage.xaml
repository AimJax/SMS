<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SocialMediaSimulator.Client.Views.ProfilePage"
             Title="Profile">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="LogoutButton" Text="Logout" Clicked="OnLogoutClicked"/>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="15">
            <!-- Profile Header -->
            <Frame Padding="20" BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource SurfaceDark}}">
                <VerticalStackLayout HorizontalOptions="Center">
                    <Frame WidthRequest="100" HeightRequest="100" CornerRadius="50" Padding="0">
                        <BoxView BackgroundColor="{StaticResource Primary}"/>
                    </Frame>
                    <Label Text="{Binding Account.Profile.DisplayName}" FontSize="22" FontAttributes="Bold" 
                           HorizontalOptions="Center" Margin="0,10,0,0"/>
                    <Label Text="{Binding Account.Username, StringFormat='@{0}'}" FontSize="14" TextColor="Gray"
                           HorizontalOptions="Center"/>
                    <Label Text="{Binding Account.Profile.Bio}" FontSize="14" 
                           HorizontalOptions="Center" HorizontalTextAlignment="Center"
                           Margin="0,5,0,0" MaxLines="3"/>

                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="30" Margin="0,15,0,0">
                        <VerticalStackLayout HorizontalOptions="Center">
                            <Label Text="{Binding Account.Profile.FollowerCount}" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
                            <Label Text="Followers" FontSize="12" TextColor="Gray" HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout HorizontalOptions="Center">
                            <Label Text="{Binding Account.Profile.FollowingCount}" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
                            <Label Text="Following" FontSize="12" TextColor="Gray" HorizontalOptions="Center"/>
                        </VerticalStackLayout>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Posts Section -->
            <Label Text="Posts" FontSize="18" FontAttributes="Bold"/>
            <ScrollView HeightRequest="300">
                <VerticalStackLayout x:Name="PostsLayout"/>
            </ScrollView>

            <ActivityIndicator x:Name="LoadingIndicator" IsRunning="False" HorizontalOptions="Center"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
